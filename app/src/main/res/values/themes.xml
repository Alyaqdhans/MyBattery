<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        Base theme — used for the launch window background only.
        We use ?android:attr/colorBackground so the OS paints the
        correct colour (light or dark) before Compose draws anything.
    -->
    <style name="Theme.MyBattery" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">?android:attr/colorBackground</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

</resources>